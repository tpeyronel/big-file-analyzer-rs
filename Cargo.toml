[package]
name = "file-analyzer"
version = "0.1.0"
edition = "2021"

[dependencies]
crossterm = { version = "0.27.0", features = ["event-stream"] }
futures = "0.3.30"
tokio = { version = "1.37.0", features = [
    "fs",
    "io-util",
    "macros",
    "sync",
    "rt-multi-thread",
] }
